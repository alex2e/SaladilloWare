<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SaladilloWare"
             x:Class="SaladilloWare.Login">
    <ContentPage.BindingContext>
        <local:ViewModels.LoginViewModel/>
    </ContentPage.BindingContext>

    <StackLayout Padding="100" HorizontalOptions="FillAndExpand">
        <Label Text="Inicio de Sesión"/>
        <Entry Placeholder="Usuario" Text="{Binding usuario, Mode=TwoWay}"/>
        <Entry Placeholder="Password" IsPassword="True" Text="{Binding contrasenha, Mode=TwoWay}"/>
        <Button Text="Iniciar Sesión"/>
    </StackLayout>
    
</ContentPage>